
const Header = ({title}) => {

    return (
        <div class="container">
        <div class="row justify-content-center">
            <h1 className="text-center">{title}</h1>
        </div>
        </div>
    )
}

export default Header